import styled from 'styled-components';

export const ShopMain = styled.div`
  position: relative;
  display: flex;
  gap: 1rem;

  & .spinner {
    position: absolute;
    left: 50%;
    top: 25%;
  }
`;
